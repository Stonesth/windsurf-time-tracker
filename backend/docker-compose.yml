version: '3.8'

services:
  time-tracker-api:
    build: .
    container_name: time-tracker-api
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      - ./config:/app/config:ro
